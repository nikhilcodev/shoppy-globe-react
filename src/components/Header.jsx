import "./Header.css"

import Navbar from "./Navbar"

/**
 * Header component — top-level page header, wraps the Navbar.
 */
export default function Header() {
	return (
		<header>
			<Navbar />
		</header>
	)
}